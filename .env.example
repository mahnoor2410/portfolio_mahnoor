SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mahnoorshahid2410@gmail.com
# Must be a real 16-character Gmail App Password (letters/numbers only).
# Google Account → Security → 2-Step Verification → App passwords
SMTP_PASS=your_gmail_app_password
PORT=5000

# Optional on Vercel: set the same SMTP_* vars in Project → Settings → Environment Variables
# If SMTP_PASS is missing/invalid, the API falls back to FormSubmit.
